import 'package:flutter/material.dart';

class AppColors {
  static const Color tabColor = Color(0xff2C2C2E);   // Dark Slate Blue
  static const Color bgColor = Color(0xff1C1C1E);    // Light Gray (unchanged)
  static const Color buttonColor = Color(0xffD0FD3E); // Bright Green (unchanged for primary buttons)
  static const Color button2Color = Color(0xff3498DB); // Vivid Blue for secondary buttons (unchanged)
  static const Color buttonTextColor = Colors.black; // White for text on buttons (unchanged)
  static const Color textFormFieldBorderColor = Color(0xffFFFFFF); // Changed to White for input borders
  static const Color textFormFieldLabelColor = Color(0xffFFFFFF);  // Changed to White for labels
  static const Color text1Color = Color(0xffFFFFFF);  // Changed to White for main text
  static const Color text2Color = Color(0xffFFFFFF);  // Changed to White for secondary text
  static const Color text3Color = Colors.orange;  // Bright Orange for accents (unchanged)
  static const Color strokeColor = Color(0xffFFFFFF); // Changed to White for strokes and borders
}
