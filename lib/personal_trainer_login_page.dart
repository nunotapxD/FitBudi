import 'package:flutter/material.dart';

class PersonalTrainerLoginPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Login Personal Trainer'),
      ),
      body: Center(
        child: Text('Personal Trainer Login'),
      ),
    );
  }
}
