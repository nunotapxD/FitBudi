import 'package:flutter/material.dart';

class ClientLoginPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Login Cliente'),
      ),
      body: Center(
        child: Text('Cliente Login'),
      ),
    );
  }
}
